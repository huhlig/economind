# Archived Provider Stubs

These files are not compiled (no `mod archive` in provider.rs).
They are retained as reference implementations for potential future connectors.
Do not add a `mod archive;` declaration — these are intentionally excluded from the build.
